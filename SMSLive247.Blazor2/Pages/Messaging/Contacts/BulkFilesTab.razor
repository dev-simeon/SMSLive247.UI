@* Bulk Files Tab *@
@* Matches React: Contacts/Index.tsx — activeTab === "bulk" *@
@* Shows BatchFilesTable filtered to bulk (non-excel) files *@

<div class="bg-white rounded-xl border border-slate-100 shadow-sm p-6">
    <p class="text-slate-500 text-sm">Bulk Files — uploaded bulk SMS files — coming soon.</p>
</div>

@code {
    // TODO: Implement bulk files table
    // React reference: BatchFilesTable tabType="bulk"
}
