@* CSV / Excel Tab *@
@* Matches React: Contacts/Index.tsx — activeTab === "excel" *@
@* Shows BatchFilesTable filtered to excel/csv template files *@

<div class="bg-white rounded-xl border border-slate-100 shadow-sm p-6">
    <p class="text-slate-500 text-sm">CSV / Excel — uploaded template files — coming soon.</p>
</div>

@code {
    // TODO: Implement CSV/Excel files table
    // React reference: BatchFilesTable tabType="excel"
}
