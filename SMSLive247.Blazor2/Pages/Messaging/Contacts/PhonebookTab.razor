@* Phonebook Tab *@
@* Matches React: Contacts/Index.tsx — activeTab === "phonebook" *@
@* Shows contacts and groups with a sub-toggle (Contacts / Groups) *@

<div class="bg-white rounded-xl border border-slate-100 shadow-sm p-6">
    <p class="text-slate-500 text-sm">Phonebook — contacts and groups table — coming soon.</p>
</div>

@code {
    // TODO: Implement contacts/groups sub-toggle table
    // React reference: viewMode === "contacts" | "groups"
}
